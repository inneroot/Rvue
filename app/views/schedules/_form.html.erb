<%= form_with(model: schedule, local: true) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p> Дата:
    <%= form.date_field :day%></p>
  </div>

  <div class="field">
    <h2>Святые</h2>
    <%= form.cktext_area :saints, class: 'ckeditor_class', ckeditor: { language: 'ru'} %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <h2>Службы</h2>
    <%= form.cktext_area :services, class: 'ckeditor_class', ckeditor: { language: 'ru'} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
